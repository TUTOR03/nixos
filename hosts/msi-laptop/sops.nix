{ self, config, hostName, userName, ... }:

{
  # sops = {
  #   defaultSopsFile = "${self}/secrets/${hostName}/host.yaml";
  #   age.sshKeyPaths = [ "/home/${userName}/.ssh/id_ed25519" ];

  #   secrets = { };
  # };
}
